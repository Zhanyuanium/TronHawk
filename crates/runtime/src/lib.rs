//! Execute plugins, provide APIs, bridge to Electron. Must NOT do user/install management. (placeholder).

#[cfg(test)]
mod tests {
    #[test]
    fn placeholder_compiles() {
        assert_eq!(2 + 2, 4);
    }
}
