//! JSON-RPC over local socket; every message carries a version and request id. (placeholder).

#[cfg(test)]
mod tests {
    #[test]
    fn placeholder_compiles() {
        assert_eq!(2 + 2, 4);
    }
}
