[package]
name = "tronhawk-core"
version.workspace = true
edition.workspace = true

[dependencies]
tronhawk-ipc = { path = "../ipc" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
